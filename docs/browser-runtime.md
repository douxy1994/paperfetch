# Browser Runtime

The supported browser runtime is the CDP workflow backed by `cloakbrowser`.

Use [`providers.md`](providers.md), [`deployment.md`](deployment.md), and [`architecture/overview.md`](architecture/overview.md) for provider-specific browser routes, environment variables, and runtime ownership boundaries.
